{
  "name": "classpilot",
  "private": true,
  "version": "0.2.0-week2",
  "workspaces": [
    "teacher-app",
    "student-app"
  ],
  "scripts": {
    "dev:teacher": "npm run dev --workspace teacher-app",
    "dev:student": "npm run dev --workspace student-app",
    "build": "npm run build --workspace teacher-app && npm run build --workspace student-app",
    "lint": "npm run lint --workspace teacher-app && npm run lint --workspace student-app"
  }
}
