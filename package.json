{
  "name": "classpilot",
  "private": true,
  "version": "0.2.0-week2",
  "workspaces": [
    "admin-app",
    "teacher-app",
    "student-app"
  ],
  "scripts": {
    "dev:admin": "npm run dev --workspace admin-app",
    "dev:teacher": "npm run dev --workspace teacher-app",
    "dev:student": "npm run dev --workspace student-app",
    "build": "npm run build --workspace admin-app && npm run build --workspace teacher-app && npm run build --workspace student-app",
    "lint": "npm run lint --workspace admin-app && npm run lint --workspace teacher-app && npm run lint --workspace student-app"
  }
}
